module;

export module UI.PhotographyPanel.MessageHandler;

import <windows.h>;

namespace UI::PhotographyPanel::MessageHandler {

export LRESULT CALLBACK static_window_proc(HWND hwnd, UINT msg, WPARAM w_param, LPARAM l_param);

}  // namespace UI::PhotographyPanel::MessageHandler
