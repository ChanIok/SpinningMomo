diff --git a/installed/x64-windows-static/include/wil/result.h b/installed/x64-windows-static/include/wil/result.h
index 8781ad54..0486422d 100644
--- a/installed/x64-windows-static/include/wil/result.h
+++ b/installed/x64-windows-static/include/wil/result.h
@@ -1030,7 +1030,7 @@ namespace details
                         auto nameSizeBytes = strlen(context.contextName) + 1;
                         size_t remainingBytes = static_cast<size_t>(callContextStringEnd - callContextString);
                         auto copyBytes = (nameSizeBytes < remainingBytes) ? nameSizeBytes : remainingBytes;
-                        memcpy_s(callContextString, remainingBytes, context.contextName, copyBytes);
+                        CopyMemory(callContextString, context.contextName, copyBytes);
                         *(callContextString + (copyBytes - 1)) = '\0';
                     }
 
diff --git a/installed/x64-windows-static/include/wil/resource.h b/installed/x64-windows-static/include/wil/resource.h
index a57b9f89..5a7d3726 100644
--- a/installed/x64-windows-static/include/wil/resource.h
+++ b/installed/x64-windows-static/include/wil/resource.h
@@ -4010,7 +4010,7 @@ string_type make_unique_string_nothrow(
         if (source)
         {
             const size_t bytesToCopy = lengthToCopy * sizeof(*source);
-            memcpy_s(result, allocatedBytes, source, bytesToCopy);
+            CopyMemory(result, source, bytesToCopy);
             result[lengthToCopy] = L'\0'; // ensure the copied string is zero terminated
         }
         else
@@ -4040,7 +4040,7 @@ string_type make_unique_ansistring_nothrow(
     {
         if (source)
         {
-            memcpy_s(result, allocSizeBytes, source, allocSizeBytes - sizeof(*source));
+            CopyMemory(result, source, allocSizeBytes - sizeof(*source));
         }
         else
         {
diff --git a/installed/x64-windows-static/include/wil/result_macros.h b/installed/x64-windows-static/include/wil/result_macros.h
index 27e687d5..ea89bb15 100644
--- a/installed/x64-windows-static/include/wil/result_macros.h
+++ b/installed/x64-windows-static/include/wil/result_macros.h
@@ -20,9 +20,17 @@
 #include "common.h"
 
 #if !defined(__WIL_MIN_KERNEL) && !defined(WIL_KERNEL_MODE)
+#ifndef _CRT_MEMCPY_S_INLINE
+#define _CRT_MEMCPY_S_INLINE inline
+#define __WIL_RESTORE_CRT_MEMCPY_S_INLINE
+#endif
 #ifdef __MINGW32__
 #include <windows.h>
 #else
 #include <Windows.h>
 #endif
+#ifdef __WIL_RESTORE_CRT_MEMCPY_S_INLINE
+#undef _CRT_MEMCPY_S_INLINE
+#undef __WIL_RESTORE_CRT_MEMCPY_S_INLINE
+#endif
 #endif
@@ -140,7 +140,14 @@ WI_ODR_PRAGMA("WIL_FreeMemory", "0")
 
 #if defined(__cplusplus) && !defined(__WIL_MIN_KERNEL) && !defined(WIL_KERNEL_MODE)
 
+// WIL header unit compatibility patch for MSVC:
+// avoid strsafe's header-inline path and avoid UCRT memcpy_s header-inline path.
+#ifndef STRSAFE_LIB
+#define STRSAFE_LIB
+#endif
+#pragma comment(lib, "legacy_stdio_definitions.lib")
 #include <strsafe.h>
+#pragma comment(lib, "strsafe.lib")
 #include <intrin.h> // provides the _ReturnAddress() intrinsic
 #include <new.h>    // provides 'operator new', 'std::nothrow', etc.
 #if defined(WIL_ENABLE_EXCEPTIONS) && !defined(WIL_SUPPRESS_NEW)
@@ -2346,7 +2353,7 @@ namespace details
             *pCopyRefCount = 0;
             if (pData != nullptr)
             {
-                memcpy_s(pCopyRefCount + 1, cbData, pData, cbData); // +1 to advance past sizeof(long) counter
+                CopyMemory(pCopyRefCount + 1, pData, cbData); // +1 to advance past sizeof(long) counter
             }
             assign(pCopyRefCount, cbData);
             return true;
@@ -3080,10 +3087,10 @@ namespace details
             return pStart;
         }
 
-        memcpy_s(pStart, bufferSize, pszString, stringSize);
+        CopyMemory(pStart, pszString, stringSize);
         assign_to_opt_param(
             ppszBufferString,
             reinterpret_cast<TString>(
                 pStart)); // lgtm[cpp/incorrect-string-type-conversion] False positive - The query is misinterpreting a buffer (char *) with a MBS string, the cast to TString is expected.
         return pStart + stringSize;
     }
